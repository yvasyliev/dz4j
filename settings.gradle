plugins {
    id 'com.gradle.develocity' version '4.4.1'
}

rootProject.name = 'dz4j'

develocity {
    buildScan {
        termsOfUseUrl = 'https://gradle.com/help/legal-terms-of-use'
        termsOfUseAgree = 'yes'

        publishing.onlyIf { System.getenv('CI') == 'true' }
    }
}
