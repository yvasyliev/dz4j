info:
  name: get-artist
  type: http
  seq: 2

http:
  method: GET
  url: "{{api-host}}/artist/{{artist-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
