info:
  name: get-editorial-selection
  type: http
  seq: 2

http:
  method: GET
  url: "{{api-host}}/editorial/{{editorial-id}}/selection"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
