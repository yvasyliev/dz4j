info:
  name: get-editorial-selection
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-host}}/editorial/{{genre-id}}/selection"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
