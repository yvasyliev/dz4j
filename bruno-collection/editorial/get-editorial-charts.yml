info:
  name: get-editorial-charts
  type: http
  seq: 2

http:
  method: GET
  url: "{{api-host}}/editorial/{{genre-id}}/charts?index=0&limit=1"
  params:
    - name: index
      value: "0"
      type: query
    - name: limit
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
