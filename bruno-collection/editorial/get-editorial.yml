info:
  name: get-editorial
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-host}}/editorial/{{genre-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
