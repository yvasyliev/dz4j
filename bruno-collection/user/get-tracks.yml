info:
  name: get-tracks
  type: http
  seq: 34

http:
  method: GET
  url: "{{api-host}}/user/me/tracks?access_token={{access-token}}"
  params:
    - name: access_token
      value: "{{access-token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
