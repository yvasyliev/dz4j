info:
  name: get-playlist-chart
  type: http
  seq: 27

http:
  method: GET
  url: "{{api-host}}/user/me/charts/playlists?access_token={{access-token}}"
  params:
    - name: access_token
      value: "{{access-token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
