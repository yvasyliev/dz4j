info:
  name: unfollow-user
  type: http
  seq: 43

http:
  method: DELETE
  url: "{{api-host}}/user/me/followings?access_token={{access-token}}&user_id={{user-id}}"
  params:
    - name: access_token
      value: "{{access-token}}"
      type: query
    - name: user_id
      value: "{{user-id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
