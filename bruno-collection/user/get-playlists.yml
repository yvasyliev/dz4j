info:
  name: get-playlists
  type: http
  seq: 28

http:
  method: GET
  url: "{{api-host}}/user/me/playlists?access_token={{access-token}}&index=0&limit=1"
  params:
    - name: access_token
      value: "{{access-token}}"
      type: query
    - name: index
      value: "0"
      type: query
    - name: limit
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
