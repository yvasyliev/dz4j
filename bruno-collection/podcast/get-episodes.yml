info:
  name: get-episodes
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-host}}/podcast/{{podcast-id}}/episodes?access_token={{access-token}}"
  params:
    - name: access_token
      value: "{{access-token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
