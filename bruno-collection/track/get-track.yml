info:
  name: get-track
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-host}}/track/{{track-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
