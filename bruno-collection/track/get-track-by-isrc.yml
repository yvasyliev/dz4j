info:
  name: get-track-by-isrc
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-host}}/track/isrc:{{track-isrc}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
