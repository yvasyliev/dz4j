info:
  name: get-podcasts
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-host}}/genre/{{genre-id}}/podcasts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
