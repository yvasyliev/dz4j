info:
  name: get-genre
  type: http
  seq: 2

http:
  method: GET
  url: "{{api-host}}/genre/{{genre-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
