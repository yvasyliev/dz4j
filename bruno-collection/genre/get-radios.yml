info:
  name: get-radios
  type: http
  seq: 5

http:
  method: GET
  url: "{{api-host}}/genre/{{genre-id}}/radios"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
