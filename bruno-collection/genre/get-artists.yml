info:
  name: get-artists
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-host}}/genre/{{genre-id}}/artists"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
