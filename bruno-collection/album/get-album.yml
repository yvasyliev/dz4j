info:
  name: get-album
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-host}}/album/{{album-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
