info:
  name: get-fans
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-host}}/playlist/{{playlist-id}}/fans?limit=1&index=0"
  params:
    - name: limit
      value: "1"
      type: query
    - name: index
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
