info:
  name: get-radio
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-host}}/radio/{{radio-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
