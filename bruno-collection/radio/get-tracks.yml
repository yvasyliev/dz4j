info:
  name: get-tracks
  type: http
  seq: 6

http:
  method: GET
  url: "{{api-host}}/radio/{{radio-id}}/tracks?index=0&limit=1"
  params:
    - name: index
      value: "0"
      type: query
    - name: limit
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
